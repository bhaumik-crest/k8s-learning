apiVersion : v1
kind : Pod
metadata :
  name : nodeapp.example.com
  labels :
    app: node-app
spec: 
  containers:
    - name: k8s-demo
      image: bhaumik20/getting-started
      ports: 
       - containerPort: 3000